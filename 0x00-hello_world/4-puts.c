#include <stdio.h>

/**
 * main - Entry point
 *  The file uses the  Puts function insted of  printf
 * Return: Always 0 (Success)
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
